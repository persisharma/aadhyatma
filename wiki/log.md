# Wiki Log

Append-only. Entries: `## [YYYY-MM-DD] <op> | <description>`
Ops: `ingest`, `query`, `lint`, `gap`, `prune`.

## [2026-06-09] ingest | bootstrap — initial repo scan, created overview + index
## [2026-06-09] ingest | cataloged existing docs in place (RULEBOOK.md, design.md, docs/roadmap, docs/superpowers) — not moved
## [2026-06-09] ingest | fixed chaptered-reader auto-advance gap (Durga/Ganesh/Saraswati/Vishnu); created subsystems/readers.md; added RULEBOOK §3 rule + readerAutoAdvance.test.tsx
## [2026-06-09] gap | reader screens have no shared shell — auto-advance must be hand-applied per reader (tracked by readerAutoAdvance.test.tsx); Ramcharitmanas single-chapter today, latent auto-advance need when a 2nd kāṇḍa lands
## [2026-06-09] ingest | re-verified readers.md against source (4 stotram screens, RULEBOOK §3, chapter counts, readerAutoAdvance.test.tsx) — all accurate, no changes
## [2026-06-13] ingest | post-#105 catch-up (5 merges): Daily Routine subsystem #87/#109 → new subsystems/routine.md; home rework #110 (Crest→HomeWordmark, docked RoutineBanner); every reader gained AddToRoutineButton; ReadingProgress.clearChapterProgress (#108); unique reminder times (#107); 9 new Maestro flows + "unit-tested instead" table — overview.md, readers.md, index.md refreshed

## [2026-06-13] ingest | Gujarati + Kannada reading-language support: new concepts/languages.md (Lang union, runtime Devanagari→gu/kn transliteration, localize/langType helpers, meaning policy, Noto serifs); overview.md fonts + reading-languages note; index.md Concepts entry. Code: utils/transliterate.ts + localize.ts + langType.ts (new), 4-lang LanguageToggle/MoreScreen radios/titleByLanguage, full app sweep; .maestro/language-smoke.yaml verified on iOS sim (gu/kn + hi/en regression via more-smoke). RULEBOOK §1/§3 + design.md §3/§3.1/§16 updated.

## [2026-06-13] verify | transliteration cross-checked vs independent authority (indic_transliteration/sanscript) over full 5,548-string corpus → 99.8% match both gu/kn; remaining diffs are sanscript Devanagari-leaks (engine correct). Corrected Kannada mapping to faithful ISO-15919 (candrabindu→ಁ, nukta→಼ preserved, क़…य़→base+಼) after verifying Noto Serif Kannada renders U+0C81/U+0CBC; updated transliterate.test.ts + design spec + concepts/languages.md. Flagged ~6 malformed nukta-on-matra source strings in Gita JSON as a separate §10 content fix.

## [2026-06-13] ingest | "everything in selected script" follow-up: meaning/commentary policy changed so gu AND kn re-script the Hindi meaning into their script (kn no longer falls back to English); meaningSourceLang now identity; fixed GitaVersePage kn fallback note + ShareCard kn meaning font + localize.test. Daily-verse notification localized: formatNotificationContent(verse, lang) + scheduler.ts lang param + NotificationPreferences provider threads persisted lang and reschedules on language change; scheduler.test asserts per-language output. Docs updated (spec, RULEBOOK §1, design.md, concepts/languages.md). Caveat recorded: meaning text is transliterated Hindi, not native translation.
## [2026-06-26] ingest | native gu/kn meaning scale-out guard: concepts/languages.md refreshed for verified `meaningGu`/`meaningKn` overrides plus transliteration fallback; contentCorrectness.test now pins completed native sections to fusion provenance and complete native rows.
## [2026-07-03] ingest | Documented Sadhana waiting-preview behavior: calendar-gated prebuilt sankalps now show the next selected unit in Today's Practice without enabling completion until active.
## [2026-07-04] ingest | Sadhana card redesign to app design language (design.md §46): RoutineShell gained an optional `background`; catalog/detail now sit on deity sketch backgrounds; program cards are warm LibraryCard-style (added `thumb` glyph to each program, both-language titles, status pill, chevron); font-face + token fixes on SankalpTodayCard/detail. New e2e sadhana-calendar-preview-smoke.yaml; both sadhana flows green on iOS sim.
## [2026-07-06] ingest | Post-1.4.3 catch-up (6 merges). New subsystem pages: subsystems/panchang.md (PRD-14 Daily Muhurat — Choghadiya/Rahu Kaal/Abhijit engine, useMuhurat hook, MuhuratDetailScreen, 4 tsx engine tests) and subsystems/japam-alarms.md (repeat days, one-time, skip-next; native AlarmKit/AlarmManager tier; expo fallback slot-cap). Sadhana fix (#173): routine.md updated with sadhana-sankalp-smoke.yaml. overview.md: module map + contexts + Maestro count refreshed (18+ flows). Assets: PNGs→WebP conversion and WAV compression are mechanical — no wiki entry.
## [2026-07-06] ingest | Sadhana/Sankalp UX review fixes: sankalp catalog gained 3 standing entry points (RoutineToday + RoutineList browse buttons, Home संकल्प spotlight — was orphaned behind the create-routine chooser); RoutineList/ModeCard cards moved to the warm §8 gradient language; app-wide Indic typography sweep (Cormorant-fallback wrong-face on hi captions, letterSpacing splitting shirorekha in chips — TheerthDetail deity pill "शि व", theerth card meta, Search headers, Japam/Reminder labels, Profile/More stats, ShareCard, Home wordmark/mantra), lineHeight ≥1.5× on Devanagari buttons/captions; checklist rows pin circle/chevron to the title line; hanuman-41 intro no longer restates the grace-rule footer. New guards: utils/__tests__/typographySafety.test.ts, screens/__tests__/SankalpTouchpoints.test.tsx, progress.test.ts intro assertion. design.md §26/§27/§31/§32/§45/§46 synced; routine.md gotchas updated.
## [2026-07-09] ingest | Kshaya tithi capture + kshaya-aware festival matching: engine detects the sunrise-to-sunrise tithi-index +2 jump and fills `PanchangData.kshayaTithi` (e.g. Ekadashi 10 Jul 2026 — 8:16 AM → 5:22 AM next day, at no sunrise); PanchangScreen Tithi tile renders it as a second row and all anga end-times past midnight gain a short-date suffix. festivalEngine `matchesLunarTithiRuleOnDate` gained a kshaya fallback (observe the day the tithi prevails, month from the kshaya day's own sunrise except month-opening pratipada) — regenerating precomputedObservances recovered ~170 silently-missing observances 2024–2031 (Yogini Ekadashi 2025/2026, Holi 2028, Vasant Panchami 2025, Karwa Chauth 2024) with zero moved/removed dates. subsystems/panchang.md + design.md §33 synced; engine/observance tests added.
## [2026-07-09] ingest | Review fixes for kshaya work: CACHE_VERSION→2 (stale per-city observance stores now purge+rescan), adhik guard no longer pre-empts the kshaya month-opening-pratipada borrow, vriddhi dedupe (monthly vrat fires once, first day — 74 duplicate dates removed 2024–2031), kshayaNakshatra added alongside kshayaTithi (engine + tiles + Muhurat card क्षय rows), shared sunriseCache kills the duplicate next-day solve, formatEndInstant/isSameLocalDay centralised in muhuratFormat.ts and consumed by tiles + MuhuratCardBody, chunked scanner warms one day past both year edges.
## [2026-07-10] ingest | Japam-alarm ring-tune fix: alarms for hare-krishna-mahamantra + gayatri-mantra rang the default chime because only om-namah-shivaya.wav was bundled. Generated 28 s WAV excerpts from the audio-library takes, registered in japam-alarm-sounds/index.ts + app.json sounds[]; japam-alarms.md gained the ring-tune section + double-registration gotcha; design.md §35 ring-tune bullet added. om-namo-bhagavate-vasudevaya still has no recording (default tone).

## [2026-07-22] ingest | Added runbooks/e2e-verification.md — Maestro authoring rules, isolated-sim verification recipe, gotchas, and the ship-with-e2e policy. Prompted by the Home redesign (#198/#200/#202) e2e audit: all Home selectors verified correct against current source; fixed _launch.yaml to dismiss onboarding before the Home canary (What's New full-modal). wiki index Runbooks section populated.

## [2026-07-22] ingest | Android alarm-volume fix: japam alarms rang on the notification stream (USAGE_NOTIFICATION v1 channels) — silenced by vibrate/silent/DnD/zeroed notification slider ("volume not coming" complaints). Moved both Android tiers to the alarm stream (USAGE_ALARM) under new -v2/:v2 channel ids (v1 deleted on ensure); Kotlin channel sound URI switched to name-based android.resource path (int raw ids renumber across updates). japam-alarms.md gotchas + design.md §35 synced.

## [2026-07-22] ingest | Android media-playback silence fix (OnePlus 15R / Android 16 reports): audioSession.ts passed both interruptionMode (iOS mixWithOthers) and interruptionModeAndroid (duckOthers); expo-audio resolves interruptionMode ?? interruptionModeAndroid, so Android got mixWithOthers → native module never requests audio focus → Android 12+ system force-mutes focus-less players when any other app holds focus (enforced hardest on Android 16 / OxygenOS 16; older devices played fine). Fixed by branching the single interruptionMode field per platform (android→duckOthers). New guard: src/audio/__tests__/audioSession.jest.test.ts (+ src/audio glob in jest testMatch). design.md §"Audio" playback-state para synced.

## [2026-07-24] ingest | Deity icons unified onto the design icon language: all 21 DeityIconKeys now render hand-drawn View-composition glyphs (17 converted from the interim emoji path; the emoji map and design.md §42 emoji exception are deleted). New structure: components/deityGlyphs/ — one file per key + palette.ts (baked illustration colors) + total Record registry (missing glyph = typecheck error); DeityIcon is a thin router with a uniform 36×36 canvas and deity-glyph-<key> testIDs. Tests: 21-key totality loop (zero Text nodes), part-testID assertions for chakra/shani/navagraha/trishul/lakshmi/narasimha; TrackCard/MiniPlayer 🏹 assertions → testID. design.md §42 rewritten; 2026-05-08 deity-icons spec gained a dated addendum. New page: concepts/deity-icons.md.
## [2026-07-24] ingest | PRD-C Kundali + Daily Rashifal: extended the pure Panchang astronomy foundation with Lahiri graha longitudes, sidereal Lagna, whole-sign houses, Vimshottari Dasha, beginner insights, and deterministic all-sign transit guidance; added permanent Home Kundali launcher, Panchang/Jyotish landing, local IST birth profile, North Indian chart, graha/Dasha views, existing-reader practice links, Swiss Ephemeris golden fixtures, targeted Jest, and an isolated-simulator Maestro smoke. Synced subsystems/panchang.md, design.md §51, and RULEBOOK §13.
## [2026-07-24] ingest | Native Maestro verification path: converted PRD-C kundali-smoke.yaml from Expo Go to the real `com.prashantsharma.vedansh` iOS debug build, with app-state reset and an explicit English-language setup; added the isolated native prebuild/install/run recipe to runbooks/e2e-verification.md and the flow catalog.
## [2026-07-24] lint | Native Kundali smoke verified on the installed iOS bundle with `RCT_jsLocation` isolation; clarified Expo Go vs native-build coverage. Wiki links have no dead targets or orphan pages.
## [2026-07-24] ingest | Stabilized the Panchang peer-mode selector: Panchang / Vrat & Parv / Jyotish is now the fixed first control in every mode, while location, lunar-system, and My Vrat controls render beneath it only in the two Panchang-derived modes. Added a structural regression test, synced design.md §33/§51 and subsystems/panchang.md, and visually compared Vrat/Jyotish on the native iOS build.
## [2026-07-24] ingest | Expanded Kundali correctness verification from three goldens to a reproducible 150-chart Swiss Ephemeris 2.10.03/Lahiri corpus (15 Indian cities × 10 IST instants, 1950–2026). Pinned official ephemeris commit/file hashes, added an isolated Python generator and engine regression test, recorded strict measured bounds and the Moon-explained Dasha-date caveat in panchang/KUNDALI_VERIFICATION.md, and synced RULEBOOK §13 plus the PRD-C TRD.
## [2026-07-24] ingest | Google Play alarm-permission remediation: Android Japam alarms no longer declare USE_FULL_SCREEN_INTENT or auto-granted USE_EXACT_ALARM and no longer attach a full-screen intent. Precise scheduling now uses user-controlled SCHEDULE_EXACT_ALARM with an explanatory Alarms & reminders banner/settings bridge; denied access keeps the existing inexact Doze-tolerant fallback. Added a policy regression test and synced design.md §35 plus subsystems/japam-alarms.md.
## [2026-07-25] ingest | Final Jyotish experience synced to the approved prototype: explicit loading/guest/saved/recovery states; no default profile or city; saved daily-first Favour/Pause/Reflect landing; actionable translated Kundali insights; Dasha progress, due time, current Antardasha and full timeline; consistent practice treatment; and privacy-distinct 4:5 Kundali/Rashifal sharing. Updated subsystems/panchang.md, design.md §51, RULEBOOK §13, tests, and native Maestro coverage.
## [2026-07-25] lint | Rechecked the 10-page wiki after the Jyotish ingest: no dead wikilinks, orphan pages, needs-review pages, or contradictions. The existing shared-reader-shell gap remains intentionally tracked.
## [2026-07-25] ingest | Single-chapter texts no longer cost two taps to open: `buildEntryStartTarget` sent every text in `stotramChaptersRouteById` to its Chapters screen, so the four that ship exactly ONE chapter (bajrang-baan, hanuman-ashtak, ram-stuti + its ram-aarti alias, ramcharitmanas) rendered a one-row "Section 1" index that had to be tapped through — on the Home आज के लिए row, the By-Purpose discovery lists, search, category/deity lists, and Rashifal alike, since all of them funnel through this one helper. Those now open their reader at `{chapter: 1, initialIndex: 0}`; 2+ chapter texts keep their index. Chapter counts derive from each text's shipped `*ChaptersManifest` (`chapterCountBySourceId`), never hand-mirrored, so a text that gains a chapter regains its index for free — the manifests were already in `texts.ts`'s import graph, so this costs no bundle weight. `navigateToProgress` follows the same rule (no one-row index pushed under the reader on resume/deep-link); `isChapteredEntry` deliberately unchanged (it reports the `<sourceId>::<chapter>` progress-key shape, which these texts keep). design.md §38 + §19 item 4 and RULEBOOK §2 row 10 synced; new `.maestro/single-chapter-open-smoke.yaml` plus entryRoutes.test.ts cases.
## [2026-07-25] ingest | Home first-touch isolation: the native splash now waits for layout-critical font-scale/language hydration; the Panchang stack is lazy-loaded; Home observance/muhurat work waits for active interactions; TodayStrip and inline RoutineBanner reserve stable height; and launcher presses recover the iOS ScrollView no-drag cancellation case. Purpose and Kundali one-touch flows passed from clean Expo Go starts; their Maestro scroll targets are centered above the tab bar. Updated overview.md and subsystems/panchang.md.
## [2026-07-25] ingest | Implemented the Claude Design "Mobile Design Review" (1 bug, 3 high, 6 medium, 3 low) against the Vedansh mobile app. Fixed the shipped font bug (4 call sites named `NotoSansDevanagari_600SemiBold`, a family never installed or loaded, so those nodes silently fell back to the system font — worst on the exported Jyotish share card). Extracted `ReaderHeader.tsx` and deleted the top bar from 20 reader + 12 chapters screens, which had drifted to two gutters, three bottom paddings, two back-button sizes and an off-token title size; added a `variant` for the reader-16 vs index-22/20 title scales. Raised 50 chrome sites to a 10pt floor, growing the two fixed chips that would have clipped. Migrated 125 raw font-family literals and 14 hand-rolled shadows to tokens, widening `elevation` to the five clusters the audit actually found and killing the off-palette `#0a0604` + `#3c1e0a` casing drift. Collapsed three text-input specs into `TextField.tsx` (`search` 44/Cormorant for content, `form` 48/Inter for data entry). Added `spacing.readingGutter` (22) and `radii.xl`, making radii a coherent 10/14/18/22. Fixed the MuhuratCardBody 600-file + `fontWeight:'700'` Android fallback risk, the no-op `letterSpacing: 0.02` on tab labels, and redrew the Bhajan tab's filled SVG note in the siblings' stroked-View grammar. All three silent-failure rules are now enforced by `no-restricted-syntax` in `mobile/eslint.config.js`, and lint is at 0 errors (6 pre-existing `react/jsx-key` false positives on `test.each` fixtures resolved so the new RULEBOOK §4 gate is honest). Three review claims did not survive verification and are recorded as corrections: the sub-44 controls all had compensating `hitSlop` (effective 64/54/72, so H-2 was visual consistency, not accessibility); JapamAlarms' "18/22/24 in one screen" is a gutter, a card's padding and a modal inset; and the "~10 components pairing 600-files with `fontWeight:'600'`" set no `fontFamily` at all. Two 40pt back buttons the review missed (Rashifal, and a 36pt one in KathaLibrary) were found. Synced design.md §2/§3/§3.0/§4/§9/§12/§15/§17/§42/§51/§52 and RULEBOOK §3/§4, plus overview.md and subsystems/readers.md.
## [2026-07-30] ingest | First-run language & size selection: the walkthrough now ends on the More hub's Language and Reading Size rows (new `languageRow`/`readingSizeRow` spotlight targets, each wrapped in a measurable View with a `scrollNodeIntoView` reveal), and the moment the tour closes `OnboardingSetupSheet` asks the fresh installer to pick both — four `LANGUAGES` radios in their own scripts + the §43 M/L pills, applied live via the shared `setLang`/`setScale`, previewed with the reader's own `verseToken`. Gated by a third `TourContext` key (`@vedansh/onboarding-setup-v`) with its own arming flag: suppressed while the tour is up, fires on complete *or* skip, never for a returning user, and re-armed by `resetTour()` so More → Show App Tour replays the full first-run sequence. `markTourCompleted` deliberately never writes the setup key. Size-pill a11y labels are qualified ("Standard reading size") because Maestro's `snapshotKeyHonorModalViews:false` reads the More rows behind the sheet. Synced design.md §37/§43/§47, RULEBOOK §6.1, concepts/languages.md; new OnboardingSetupSheet jest suite + TourContext gating tests, `_launch.yaml` dismisses the sheet, and feature-tour-e2e walks the two new steps then drives the sheet to Begin.
## [2026-07-31] ingest | Read Aloud (पाठ सुनें): on-device TTS via `expo-speech` ~14.0.8 on the Gita and chalisa readers, answering "can we read using Apple TTS, and is there an Android equivalent" — one code path over `AVSpeechSynthesizer` and `android.speech.tts.TextToSpeech`, no config plugin or permission entry (the package ships its own `TTS_SERVICE` `<queries>` manifest). It complements rather than replaces PRD-02's bundled recitation, which has 5 real recordings for 13 catalog tracks: TTS is the fallback for the ~15 sections that will never get one. New pure `src/readAloud/` layer (`verseAdapter` duck-types all 7 verse shapes and rejects chapter sentinels first, `verseScript` emits one utterance per verse line so the inter-utterance gap lands where a reciter breathes, `pronounce` maps dandas to sentence stops **only** on the synthesizer string, `voices` resolves locale+voice); `ReadAloudPrefsContext` (`@vedansh/read-aloud`, meaning ON by default per the product call) split from the per-utterance-volatile `ReadAloudContext`; `_useReaderReadAloud` + `ReadAloudButton` in `ReaderHeader.right`; a More → Read Aloud row + sheet mirroring `ReadingSizePickerSheet` and reusing its `READING_SIZE_SAMPLE` as the spoken preview; `RateStepper` extracted from `JapamAudioPlayer`'s tempo block (second use site earned it). Mutual exclusion between the three sound sources moved into a new module-singleton `src/audio/playbackArbiter.ts` rather than a context field — deliberately, because a context field would have broken all 6 screen suites that stub `useAudioPlayerContext`. Four decisions worth remembering: (1) v1 resolves TWO speech locales, not four — gu/kn speak the Devanagari source and `meaningHi` with a `hi` voice even where authored `meaningGu`/`meaningKn` exists, because their on-screen script is a runtime transliteration a Hindi voice cannot pronounce at all; disclosed in-script in the sheet. (2) `Speech.pause`/`resume` are never called on either platform — Android's native module has neither, so pause is built from the chunk loop and is line-granular everywhere. (3) Both platforms fall back to the device default voice for a missing language SILENTLY, so availability comes from a probe plus a 3s `onStart` watchdog and never from `onError`; "unavailable" is a first-class muted state with an Android TTS-settings hop. (4) `speakOptionsFor` omits Android's `language` whenever a probed voice id exists, because Java's single-arg `Locale('hi-IN')` yields language "hi-in" → `LANG_NOT_SUPPORTED` → silent en-US fallback. Two real bugs were caught while testing: an uncleared probe-timeout timer that setState'd after unmount, and 4 reader suites failing to *run* once `expo-audio` became a transitive import (both `expo-speech` and `expo-audio` are now globally stubbed in `jest.setup.js`). 68 new tests across 7 files (710 total, all green), `read-aloud-smoke.yaml` with its device-voice-dependent half marked optional, and design.md §53/§54 + §34/§37/§43 cross-refs, RULEBOOK §3 bullets + new §11.14 "synthetic recitation is assistive, never authoritative", new wiki/subsystems/audio.md. Ships as a store release (1.4.6) — native module, no OTA.
## [2026-07-31] ingest | Read-aloud reworked to speak only genuinely supported languages, per the product call to drop the Hindi-substitution compromise. `speechLangFor` is now identity (hi→hi-IN, en→en-IN, gu→gu-IN, kn→kn-IN) and `SPEECH_LOCALE` gained gu/kn, so the spoken text comes straight from the same `verseLinesByLang`/`meaningByLang` the page renders with — authored `meaningGu`/`meaningKn` included, which the old collapse-to-Hindi path silently discarded. A language the device has no voice for now reports **unavailable**, named in its own script (new `languageName(lang)` helper off `LANGUAGES.nativeLabel`) with the Android TTS-settings hop, instead of being read in another language's voice; `voiceByTarget` holds one slot per reading language and `parsePrefs` is driven off a new `SPEECH_TARGETS` list so a future language can't be silently dropped. Two real defects surfaced while writing the tests for this, both of which would have re-opened exactly the failure the rework exists to prevent: (1) `resolveVoice` honoured a saved `preferredIdentifier` **without checking it still speaks the requested language**, so a stale/reused id could read Kannada text in a Gujarati voice — it now requires a primary-subtag match; (2) the controller's `start()`/`speakPreview()` never checked availability at all, so an unsupported language was still handed to the engine, which (per the documented silent-fallback behaviour of both platforms) would read it aloud in the device default voice with no error — they now refuse outright via an `availabilityRef`. The earlier "never calls speak while unavailable" test had been too weak to catch this: it asserted only that `availability` stayed `unavailable`, not that `speak` went uncalled. Net: 723 tests green (up from 710), 13 new/rewritten cases covering per-language resolution, the no-cross-language-fallback guarantee, and the four-target prefs shape. design.md §53.1/§53.4/§53.5, RULEBOOK §3 (new no-substitution bullet), and subsystems/audio.md updated; the gu/kn "you will hear Hindi" disclosure is gone from the settings sheet, replaced by a line naming which language the voice list configures. Trade-off now documented rather than hidden: gu/kn read-aloud depends on that voice being installed, which is less common than Hindi.
## [2026-07-31] ingest | Added the Vālmīki Rāmāyaṇa Granth section (`valmiki-ramayan`): a **declared curated selection** of 27 internet-verified ślokas across 6 kāṇḍa chapters (Bāla 7 · Ayodhyā 4 · Araṇya 3 · Kiṣkindhā 3 · Sundara 4 · Yuddha 6), on the standard chaptered-Granth pipeline (chapters index → paged reader with the kāṇḍa-boundary auto-advance contract). Content constraint worth recording: the session's egress policy blocked every Rāmāyaṇa text host (valmiki.iitk.ac.in, valmikiramayan.net, sanskritdocuments.org, sa.wikisource.org, ambuda.org, gretil, archive.org — only GitHub is reachable), so each verse was verified through web-search result text quoting ≥2 independent sources rather than direct fetch; **Uttarakāṇḍa was deliberately omitted** because no śloka in it cleared that bar, and padding the count with unverified text would violate §10.5. That produced a new RULEBOOK §10.5 carve-out: an over-large text may ship as a selection only if `sub`/`subEn` say `चयनित`/`selected`, each JSON's `source.notes` says it is a selection, and every verse still clears §10.1–§10.4 — what stays banned is a partial text the UI implies is complete. Two edition-numbering variants are recorded in `source.notes` rather than silently picked (3.37.2 also circulates as 6.16.21; 6.109.25 is 6.99.39 in the critical edition), and the one verse whose in-sarga index differs between editions cites the sarga only (`श्लोक · ३.५०`). Reader background is per-kāṇḍa via `stanza = kāṇḍa`. `ValmikiRamayanVersePage.tsx` is the RULEBOOK §2-row-4 explicit re-export of `SundarkandVersePage`. Wired into texts/entryRoutes/HomeStack/backgrounds/searchIndex/versePool/formatLocation; new per-kāṇḍa reader smoke test plus cases added to readerAutoAdvance, readerTypeScale, chapteredTotals, searchIndex, entryRoutes and contentCorrectness (the last asserts the §10.5 declaration, ≥2 sources per file, and lines↔linesEn parity). Synced design.md §53 (new) plus the §15 chapters-index note and the data-shape-families / search-index-coverage / verse-pool / last-read-vocabulary mirrors, RULEBOOK §8/§10.5, and `.maestro/granth-smoke.yaml` + its README row. E2E not run: this container has no simulator/emulator and no `maestro` binary.
## [2026-07-31] ingest | Vālmīki Rāmāyaṇa follow-ups from review. (1) All **7 kāṇḍas** now ship, not 6 — Uttarakāṇḍa was added once Rāma's boon to Hanumān (`मत्कथाः प्रचरिष्यन्ति यावल्लोके हरीश्वर…`) was attested in Devanagari on the sanskritdocuments Rāmāyaṇa mirror; it is cited at sarga level (`७.१०८`) because the Gita Press family numbers that sarga 108 while the mirror groups it as 98. Chapter depth is deliberately uneven (Uttara = 1 śloka) and RULEBOOK §10.5 now says uneven depth is correct when that is what verification supported — padding a subsection to even the counts is the reject. (2) **Naming is Vālmīki's, not Tulsidas's:** kāṇḍa 6 is युद्धकाण्ड; लंकाकाण्ड is the Rāmcharitmānas name for the same book and is recorded as an alias in chapter-06's `source.notes` rather than displayed. (3) Real UI defect found by dumping the rendered tree: `ValmikiRamayanChaptersScreen` used `GitaChapterCard`'s **defaults**, so kāṇḍas rendered as `अध्याय 1 · … · 7 श्लोक`. The card already had `chapterLabelHi/En` + `unitLabelHi/En` props — the screen just wasn't passing them. Now passes काण्ड / Kanda. Worth generalising: any new chaptered section whose unit is not the Gita's अध्याय must pass these four props, and rendering the tree (not just asserting a verse string) is what surfaces this class of bug. (4) New RULEBOOK §10.2 clause: cite only what you actually read. The user supplied the Gita Press edition (archive.org scan + ISKCON Desire Tree PDF) as the verification source, but both hosts are 403 through the egress proxy — so they are recorded as `source.canonicalEdition` / `canonicalEditionUrls` / `canonicalEditionStatus` (page-level check OUTSTANDING) instead of being listed in `referenceUrls` as though consulted, and contentCorrectness.test.ts now fails if that pending block is deleted rather than cleared. Section total 27 → 28 ślokas.
## [2026-08-01] ingest | Expanded Vālmīki Rāmāyaṇa from 28 anchors to the complete declared Southern-recension digital corpus: 23,289 unique verse records across 648 sargas and seven kāṇḍas, with Sanskrit, deterministic IAST, Hindi and English meanings. Added a pinned corpus generator and explicit repair provenance; moved Home/Daily/search to a 28-row projection while loading reader data one kāṇḍa at a time; verified all records structurally, both platform bundles, and a native Maestro path through all seven counts into Uttara Kāṇḍa 7.1.1. Updated overview, readers, and e2e-verification.
## [2026-08-01] lint | Rechecked all 10 wiki pages after the complete Vālmīki ingest: no dead wikilinks, orphan pages, needs-review pages, duplicate coverage, or contradictions; historical curated-selection log entries remain append-only and are superseded by the new ingest entry.
## [2026-08-05] ingest | Fixed the trimmed bottom line on the Kundali share card (user report, Hindi). Two independent causes, both now guarded. (1) Three 10 pt micro lines in `JyotishShareCard` shipped at `lineHeight === fontSize`, which sits the first baseline high enough to slice the top off the line — the method footer lost its shirorekha and the header rendered `जन्म कुंडली` as "जन्म कुंडला". Raised to 14 (1.4×, the ratio design.md §3.0 already recorded for the calendar `dateTag`), and the header meta — which named Inter, a face with no Indic glyphs, so its true metrics came from an unpredictable OS fallback — now routes through `scriptTitleFont` via a new `CardMeta`. (2) The chart was sized `min(208, width × 0.61)` inside a card whose height is pinned at 4:5 with `overflow: 'hidden'`, while all surrounding chrome is fixed-point-size type; on any card below ~334 dp (a 360 dp Android gets 312) the stack overran the box and the `marginTop: 'auto'` footer was the piece pushed out. `kundaliChartSize()` now gives the diagram the leftover height (content height − a 196 dp chrome budget) under the same historic cap. Both invariants pinned by a new `jyotishShareCardFit.test.tsx` (13 cases, all four languages; verified failing against the old code) and the footer line is asserted in `kundali-smoke.yaml`. Synced design.md §3.0 + §51. Recorded gap: the Rashifal card's chrome is entirely fixed-height (~375 dp) so it has no comparable slack on ≤ 360 dp phones — it fits at 334 and its leading is fixed, but the guidance-row block wants the same treatment. E2E not run: this container has no simulator/emulator and no `maestro` binary (flow parses under js-yaml).
## [2026-08-07] ingest | Location selector grew from 52 entries to 394: `MAJOR_CITIES` (the previous nationwide list, Ujjain still first because `DEFAULT_LOCATION` is `CITIES[0]`) plus a new `panchang/rajasthanTehsils.ts` carrying 342 Rajasthan tehsil headquarters across all 33 revenue districts. Data provenance matters here because this container's egress allows only package registries — no gazetteer, census or Wikipedia host is reachable — so coordinates were resolved from two datasets pulled off npm (GeoNames `cities.json` admin1=24 first, then the All-India Pincode Directory's 11,032 Rajasthan post offices) and every row was gated on the Rajasthan bounding box, its own district's 2nd–98th-percentile office box (+0.35°), lat≠lon, and coordinate uniqueness. Those gates matter: the postal directory geocodes some offices onto a same-named town in another district (Uchchain landed 5° west of Bharatpur) and snaps others onto their district HQ, so 28 tehsils ship absent rather than approximated — a wrong coordinate would serve another district's sunrise under the user's own town name. Elevations are inverse-distance interpolations from 51 published town elevations (exact for those towns); elevation only feeds sunrise's horizon-dip term, where 100 m moves sunrise well under a minute. `City` gained optional `districtHi`/`districtEn`, which is both the tier discriminator (the picker splits `CITIES` at the first entry carrying a district) and the row's disambiguator, since tehsil names repeat across the state (Rajgarh, Ramgarh, Shahpura, Kishangarh, Raipur) — ids are therefore `rj-<slug>` plus `-<district>` only on collision, and they are AsyncStorage + observance-cache keys, so they must never be renamed. New shared `cityMatchesQuery` searches name in either script plus district, so "alwar" surfaces all 21 Alwar tehsils; both pickers use it. `LocationPickerModal` now renders two labelled groups instead of one flat 394-row scroll, and the Kundali birth-city sheet moved from a mapped `ScrollView` (which mounted every row) to a `FlatList`. No `CACHE_VERSION` bump — adding cities changes no matching logic. Synced design.md §33's Location Picker section and subsystems/panchang.md; six new cases in `location.test.ts` pin the tier split, the rj- id prefix, 33 districts, in-state coordinates, coordinate uniqueness and district search. Full suite green (619 jest + 107 engine + 43 data, typecheck clean). E2E not run: this container has no simulator/emulator and no `maestro` binary.
## [2026-08-07] ingest | New default-ON festive reminders: a fourth local-notification family (`festive-reminder`) that pushes one notification on each of 18 famous festivals with festival-specific copy inviting the user to read a named bundled text, and opens that text on tap. Shape follows the three existing families exactly — data-only curated catalog (`notifications/festiveReminders.ts`, no imports from festivals.ts/texts.ts so the planner stays pure) → pure planner (`festiveReminderPure.ts`) → thin expo glue (`festiveScheduler.ts`) → headless `<FestiveReminderScheduler>` in App.tsx. Decisions worth recording: (1) the cap is **soonest-first**, the inverse of the vrat planner's followed-first rule — nobody opted into these, so a festival three days out must not lose its slot to a more famous one four months out, and fame order is only the same-instant tie-break; (2) title = the festival's own name, never concatenated, because §3.0's 10pt/conjunct-slicing trap makes a truncated Devanagari title read as a bug — the customised message lives in the body as `<greeting> · <invite>`, through `contentByLang` so gu/kn re-script; (3) fire time is 07:30, thirty minutes off the 07:00 daily-verse default so the two never land in the same instant; (4) a 120-day window (vs the daily verse's 30) because festivals are ~monthly and a six-week-absent user should still get Diwali; (5) **a festive tap opens a reader on purpose** — the opposite of the daily-verse rule that deliberately stays on the Daily Bhakti tab to protect the resume position — because the body is an explicit invitation to read a named text, and it routes through `buildEntryStartTarget` so it behaves exactly like tapping that text's own card, with a `library.find` validation + ObservanceDetail fallback since a notification armed four months ago can outlive an OTA rename; (6) festivals with no honest content match (Raksha Bandhan, Bhai Dooj) are simply absent rather than aimed at a loosely-related text. `festiveRemindersEnabled` defaults true and an absent key in a pre-feature prefs blob also resolves true, so upgraders are enabled like fresh installs; a hard OS denial flips it off alongside the daily verse. Narrowed the daily-verse reconcile effect from the whole `prefs` object to `dailyVerseEnabled` + `times` so an unrelated toggle can't reschedule the 30-day window. Own Android channel `festive-reminders` (fresh id — attributes are pinned at creation). New `wiki/subsystems/notifications.md` covers all four families, including the recorded gap that they are collectively over-subscribed against iOS's 64 pending limit in the worst case (daily verse alone can claim all 64) with festive deliberately the smallest slice and re-arming last. Synced design.md §38. Tests: festiveReminderPure.test.ts + festiveReminders.test.ts (tsx, wired into test:data), festiveScheduler.jest.test.ts, four new deepLink.jest cases; all suites pass (674 jest + 107 engine + 45 data, typecheck clean, lint unchanged at the one pre-existing `react/no-children-prop` error in TilePressContext.test.tsx). **Pre-existing gap found, not introduced here:** `npm test` exits 1 after `test:readers` and therefore never reaches `test:engine`/`test:data`, because `LocationPickerModal.test.tsx` leaks a VirtualizedList `_updateCellsToRender` timer and emits four "Cannot log after tests are done" warnings; reproduced identically on a stashed tree (108 suites / 663 tests all passing, exit 1). The tsx suites were run directly to verify. Fixing that leak is its own change. `reminders-smoke.yaml` asserts the new card's presence only — Maestro can neither drive a notification tap nor flip a toggle without raising the native permission dialog. E2E not run: this container has no simulator/emulator and no `maestro` binary.
## [2026-08-07] gap | `npm test` (RULEBOOK §0's unit gate) exits 1 after `test:readers` and never runs `test:engine`/`test:data`: `components/__tests__/LocationPickerModal.test.tsx` leaves a VirtualizedList `_updateCellsToRender` timer pending, so four `console.error` act(...) warnings arrive after the suite finishes and Jest fails the run even though all 108 suites/663 tests pass. Predates the festive-reminders change (reproduced on a stashed tree). Fix is to flush/unmount the FlatList inside `act` (or fake timers) in that suite; until then the engine and data suites must be run directly.
## [2026-08-07] ingest | Festive-reminder follow-up — the tap now lands on **Home**, and Home's FOR TODAY row leads with that festival's content. Three changes. (1) `deepLink.ts`: `festive-reminder` dispatches `HomeTab → { screen: 'Home' }` instead of `buildEntryStartTarget(sourceId)`; the message copy is untouched. Rationale, same instinct as the `daily-verse` rule: a tap made from a lock screen should not run a reader's `setProgress` effect and clobber the resume position, the day's Panchang strip and routine banner arrive with the reading, and a notification armed four months ago cannot strand the user on OTA-renamed content because Home recomputes today from today. `{ screen: 'Home' }` is explicit — focusing `HomeTab` alone restores whatever screen the stack was left on, possibly several readers deep. The payload guard now gates on `ruleId` only; `sourceId` stays as the record of what the message promised but no longer routes. (2) `getTodayRecommendationsForDate` reordered into four explicit tiers — curated festive catalog → observance `linkSectionId` → `bestFestivals` metadata → vaar deity — so a festival day leads with the occasion while an ordinary day is byte-identical to before (the weekday tier used to run first, which on a festival day pushed the festival's own reading toward the tail of a list capped at 8 and rendered at 6). Also wrapped the observance lookup in try/catch so a day the calendar cannot solve degrades to the weekday tier instead of throwing through Home's render. (3) New `getTodayRecommendationDetails` returns `{ entry, festivalHi?, festivalEn? }` so the card can render `आज दीपावली है` / `Today is Diwali` in place of the generic recommended line; `getTodayRecommendationsForDate` survives as a `.map(r => r.entry)` view so existing callers and `contentCorrectness.test.ts` are untouched. The catalog is now load-bearing in both `festiveScheduler` and `data/discoveryMeta.ts` — that is the point (one source, so the notification and the homepage cannot disagree) and it is why `festiveReminders.ts` must stay import-free: `discoveryMeta` already pulls in `texts.ts` and `festivalEngine`, so any back-reference would close a cycle. Pinned by a new loop in `festiveReminders.test.ts` that resolves each of the 18 festivals' own date and asserts the FOR TODAY row both contains its `sourceId` (attributed to the festival) and leads with a festival card, plus an ordinary-Tuesday control; `sourceId`s are now also asserted `active` and not `hidden`, since the row silently drops either. `deepLink.jest.test.tsx` rewritten for the Home landing (including a stale/absent `sourceId`), `TodayRecommendationsRow.test.tsx` gains a festival-copy case and its mock renamed `mockRecommendations` (jest.mock factories only reach `mock*`-prefixed out-of-scope variables). Synced design.md §38 (both the festive block and the deep-link table) and §50's For Today. 675 jest + 107 engine + 45 data pass; typecheck clean; lint unchanged. No Maestro change: the row's festival branch only fires on a festival date, so it is not deterministically assertable — `_launch.yaml` asserts only the unchanged `आज के लिए|FOR TODAY` eyebrow.
## [2026-08-08] ingest | Festive Toran shipped (Option A of the four prototyped festive-Home treatments; festive-theme-preview.html rewritten as the pre-implementation gallery showing the header for all 18 festivals with real catalog greetings + engine-resolved next dates, republished to the same artifact). New `components/FestiveToran.tsx`: on each of the 18 catalog festivals Home hangs a garland below the wordmark — one stretched SVG string (`M-4 6 Q150 44 304 6`, preserveAspectRatio none), 5 View-composed marigolds (8 rotated petals, cardThumbActiveFrom/To around a saffronDeep core — pushpa-varsha's blossom grammar, no emoji) alternating with 4 gold leaves at stations `y = 6 + 19·sin(πx/300)`, and a goldChipBg chip carrying the catalog greeting at 12pt via titleScriptFont/contentByLang. Motion: single ±0.7° sway, 6 s alternate, transformOrigin '50% 0%', native driver; useReducedMotion hangs it still. Fixed TORAN_HEIGHT=74 so it can never nudge the Today strip; scrolls away with the wordmark (deliberately unpinned). Garland is a11y-decorative; the chip text is the one accessible element. Driver: new `getTodayFestival(date)` in discoveryMeta — first observance in the festive catalog, walking getObservancesForDate in the same order as the FOR TODAY tier-1 loop, so toran, leading card, and the morning notification always name the same festival even when two catalog festivals share a civil day; engine throw → null → no garland. HomeScreen keys it off useTodayKey so day rollover re-resolves without relaunch. Scope locked with the user: all 18 festivals (not star-marker majors only), no pushpa-varsha one-shot (Option C parked), utsav banner rejected as duplicating the FOR TODAY card. Gotcha worth keeping: a jest suite mounting FestiveToran MUST fake timers (the sway's Animated.loop otherwise holds a real timer and jest never exits — same trap RoutineCelebration.test.tsx already documents); and TestRenderer findAll on testIDs needs a host-element filter (`typeof n.type === 'string'`) or every ornament counts twice. Tests: FestiveToran.test.tsx (6 cases: ornament census, reserved height, hi face, en/gu re-scripting + faces, decorative a11y, reduce-motion mount) + a getTodayFestival block in festiveReminders.test.ts (all-18 date loop asserting catalog greeting + FOR TODAY-lead agreement, Diwali → diwali, ordinary Tuesday → null). Synced design.md §18 item 2, §38 festive block, new §55; wiki notifications page. E2E deliberately none — festival dates are non-deterministic under clearState (same rationale as the §54 auto path). Suite: 681 jest + 107 engine + 45 data pass, typecheck clean, lint unchanged (the one pre-existing TilePressContext error).
## [2026-08-08] ingest | CI on PR #241 was red with all 681 tests passing — root-caused and fixed the 2026-08-07 "npm test exits 1" gap. Mechanism confirmed by minimal repro (3 suites, 26 tests, all green, exit 1): `LocationPickerModal.test.tsx` never unmounted its rendered trees, so the picker FlatList's VirtualizedList cell-batching timeouts outlived the suite and fired into later ones as "Cannot log after tests are done" — which this Jest setup treats as a run-level failure even when every test passes. Fix: track every rendered tree and unmount in afterEach inside act (unmount clears VirtualizedList's pending timer) — the same leak class as the FestiveToran Animated.loop, now documented twice: ANY suite that mounts a timer-scheduling component (VirtualizedList/FlatList, Animated.loop) must either unmount its trees or fake timers, or it can fail the whole run from outside itself. The in-test JapamCounterProvider act() warnings in the CI log were verified to be buffered console noise attributed to a running test, not run-failing — left alone. Second, independent flake hardened in the same pass: `dayAngaResolver.jest.test.ts`'s two full-window tests do 31 days of real sunrise+tithi bisection and blew jest's 5 s default on a loaded container (passed on CI, failed locally under load) — both now carry explicit 30 s timeouts. Consequence worth recording: `npm test` completes end-to-end for the first time, so CI now actually runs `test:engine` (107) and `test:data` (45) — the jest exit-1 had been short-circuiting the && chain, meaning those suites were silently absent from every previous CI run.
## [2026-08-09] ingest | PRD-16 Event Muhurat Finder Phase 1: pure eventMuhurat/abujhMuhurat engines (draft rule tables pinned unverified, chaturmas kshaya-safe + amanta-normalised, asta via kundali longitudes), useMuhuratFinder chunked scan, 4 Panchang-stack screens (ranked results with empty-with-reason; answer-first day detail), Home मुहूर्त tile (grid 15→16, नित्य साधना full-width closer) + Panchang door, 12 tsx engine tests + 5 jest smokes + muhurat-finder-smoke.yaml (sim run pending). design.md §18/§33/§53, RULEBOOK §14 synced.
## [2026-08-09] ingest | PRD-16 slice 2: finder share card (MuhuratFinderShareCard, off-screen capture, non-excluded days only) + month-view muhurat overlay (PanchangHome muhuratOverlay param → ringed days on the shipped calendar grid, auto-expand, dismissable chip). Jest smokes extended; e2e flow gains share-visible + overlay-chip steps.
## [2026-08-10] ingest | Dotted-circle (U+25CC) root cause + Devanagari well-formedness gate. A reader-reported `◌` in BG 14.26 (`भक्ितयोगेन`, i-matra after the virama) turned out not to be a regression: replaying the validator over every commit that ever touched content shows the defect count strictly monotonic — 98 from the first Gita import (#191), 155 after the full Valmiki corpus (#232) — **no commit ever reduced it**. Root cause is structural, not a bad edit: every script check in the repo is a *character-set membership* test (`contentCorrectness` §5's `DEVANAGARI_RANGE`, RULEBOOK §11.12's gate, `build-valmiki-ramayan.py`'s "non-Sanskrit export artifact" guard all ask only whether a codepoint sits in U+0900–U+097F), and an orphaned matra is a legal codepoint in an illegal position, so it passes all of them. There is no U+25CC in the data to grep for; the defect is the sequence. Compounding it: §11.12 gated only the romanization *direction*, which is why the Valmiki builder's `ऺ→र` map went into `devanagari_to_iast()` and left the Devanagari `lines` corrupt (7.16.48 shipped `linesEn "kṣhatritrayān"` beside `lines "क्षत्ऺित्रयान्"`), and why the two `fix-gita-*corruption.mjs` passes (which did fix `?`-as-comma, Bengali danda `৷`, missing spaces from the same dirty scrape) never touched this class. The July gu/kn spec surfaced the nukta subset, sized it at "~6 Gita source strings", and deferred it — actual shipped count 47. Inventory: **205 instances** — 107 in shipped `mobile/src/data` (23 files), 98 in the generator inputs (`BhagwadGita/chapters/*.md` 49, raw scrape 49); six kinds, dominated by matra-after-virama (91, a legacy visual-order encoding converted without reordering the i-matra past the conjunct) and nukta-after-matra (89, legacy `ृ`+nukta faking `ॄ`, plus stray nukta in Hindi prose). New: `data/devanagariWellFormed.ts` (one canonical cluster-grammar rule), `data/__tests__/devanagariWellFormed.test.ts` (sweeps every string in every content JSON, reports the exact field path, wired into `test:data`), `devanagariWellFormed.baseline.json` (shrink-only debt ledger of the 107, fails on stale entries so it cannot rot), `mobile/scripts/verify-devanagari.mts` + `npm run verify:devanagari` (scans the generator *inputs*). `parse-gita.mjs` — a whitespace-only pass-through, which is why hand-patching its JSON never held — now refuses to regenerate while the source markdown is dirty; `build-valmiki-ramayan.py` gained `assert_devanagari_well_formed()` per verse. Deliberate non-fix: the `ऺ→र` map is NOT applied to the Devanagari path, because it would yield `क्षत्रित्रयान्` where the verse means `क्षत्रियान्` — normalizing a defect away converts visibly-broken scripture into invisibly-wrong scripture (§11.3). New RULEBOOK §11.14; §11.12 now states it gates the romanization only. The 107 existing instances are quarantined, not yet fixed.
## [2026-08-10] ingest | Added Guna Milan as a fourth Jyotish tool. The versioned, pure Ashtakoota engine uses the existing Lahiri sidereal Moon and pinned North-Indian matrices; birth inputs are IST-only and require no location. Unknown time enumerates all score-relevant boundaries across the complete IST civil day and returns an exact result only when every combination agrees, otherwise a min–max range without a band or share action. Storage is explicit opt-in, diagnostics stay local, and the share model is an allow-list that cannot accept birth date/time/location. Added independent published golden rows, exhaustive longitude-boundary and pair-matrix tests, an accessible screen test, and one platform-neutral Maestro flow to run separately on iOS and Android.
## [2026-08-10] lint | Rechecked wiki links and review markers after the Guna Milan ingest; no dead wikilinks, unresolved review markers, or contradictions found.
## [2026-08-10] ingest | Guna Milan review correction: restored B. V. Raman's directional Yoni matrix instead of treating it as symmetric (Horse-row/Deer-column 3 vs reverse 1; Lion-row/Buffalo-column 2 vs reverse 1). Unknown-time ranges now list localized possible nakshatras for each affected role, and exact expanded rows translate every class, animal, lord, rashi, and nakshatra rather than exposing enum keys or zero-based indexes. Regression tests pin both matrix orientations and all four reading scripts.
## [2026-08-10] ingest | Guna Milan persistence privacy follow-up: draft saves and clears now use a failure-resilient serialized queue plus generation invalidation. Every explicit Remember-off action queues removal even if the UI has not yet observed a completed save, so a delayed AsyncStorage write cannot resurrect opted-out birth details. A deferred-write regression test pins the save-in-flight → opt-out → save-resumes race.
## [2026-08-10] ingest | Added the Home Widgets subsystem: one fail-closed v1 document planned in JavaScript (14-day IST Panchang + deterministic verses + Japam snapshot), atomic Android SharedPreferences/iOS App Group transport, generated WidgetKit/AppWidget targets, exact cold/warm deep links, gallery/pin UX, and the boundary between Maestro app-flow coverage and real launcher/widget rendering evidence. New `subsystems/home-widgets.md`; index updated.
## [2026-08-13] ingest | Added the PRD-17 Namkaran Phase 1 scaffold to the Panchang/Jyotish stack: a pure Lahiri Moon-longitude → charana engine, whole-IST-day unknown-time bisection, session-default newborn privacy, id-only shortlist, exact/range answer grammar, lazy dev corpus, rashi/muhurat/deity/share doors, and a 27-choice Home-style launcher grid fixed at 3 columns × 9 rows (one-point width slack prevents iOS pixel rounding from wrapping the third tile). The grid is name-first: each current-language nakshatra name fits inside its tile over at most two lines, with no ordinal-number title. The iOS smoke journey passed on a fresh iPhone 17 Pro Max / iOS 26.5 and also exposed/fixed the name-row shortlist control being swallowed by its accessible parent. Release remains blocked: the 108-cell religious convention is `verified:false` and the small development corpus is `releaseEligible:false`; Android smoke is still required separately.
## [2026-08-13] lint | Rechecked wiki links, review markers, and contradiction markers after the Namkaran ingest; all 10 wikilink targets resolve, no page is marked needs-review, and no contradiction marker is open.
## [2026-08-13] ingest | Customer-copy boundary: removed on-device/offline/internet/account/storage/version/DRAFT-review implementation disclosures from Namkaran, Guna Milan, Kundali/Jyotish, reminder, and share surfaces while retaining internal privacy and release gates. RULEBOOK §3 now requires action/outcome copy, PRD-17 Phases 2–3 inherit the rule, and the Panchang wiki records that Namkaran release eligibility remains test- and metadata-gated rather than shown to customers.
## [2026-08-13] ingest | Completed Namkaran prototype points 4–8 in the result experience: fixed-box hero with a collapsed Lahiri disclosure; independent 44 pt shortlist star with fill/tint state; bilingual name-detail sheet with copy-only-name and deity navigation; nine-sound rashi grid; and the existing Namkaran Muhurat hand-off. Targeted Jest, engine/data tests, typecheck, lint (baseline warnings only), and direct iOS simulator inspection covered the path.
## [2026-08-13] lint | Rechecked wiki links, page status, and contradiction markers after the Namkaran result update; all indexed wikilink targets resolve, no page is marked needs-review, and no contradiction marker is open.
## [2026-08-13] ingest | Namkaran screenshot follow-up: capped dynamic-type expansion for navigation and dense controls, strengthened the ReaderHeader line box, made empty filter states resettable, and added the approved Ashwini/Chu → Chudamani prototype record so that path has a real result. Targeted screen tests now pin both recoveries.
## [2026-08-13] ingest | Documented the Annual Answer UI reuse boundary: shared quiet Observance detail/list and Panchang timeline primitives now serve Pitru Smaran without adopting Muhurat's promotional ListCard treatment. The civil-date branch now reuses CalendarDatePicker while the lunar tithi selector remains purpose-built; the dedicated day chip, local annual fact panels, routes, calculations, content, and persistence remain unchanged.
## [2026-08-13] ingest | Completed the Pitru Smaran prototype inventory: per-person opt-in annual reminders, default-on public Pitru Paksha notices, pinned Panchang catalog row, Home and Panchang public/private day chips, and annual plus mapped-Paksha matching. Kept all family data on-device and identifier-prefixed; verified the native iOS Release build and expanded Maestro flow on the dedicated Aadhyatma-Saskatoon-Pitru-Verify simulator.
## [2026-08-13] lint | Rechecked wiki links and review markers after the Pitru Smaran completion ingest; no dead wikilinks or unresolved review markers found.
## [2026-08-13] ingest | Clarified Pitru Smaran discovery and reminder defaults: added an always-present Home DISCOVER card, made the zero-entry Panchang ledger non-dismissible, and defaulted new per-person reminders on only after the shared OS notification grant succeeds. Also strengthened the add-mode segmented control with a separately bounded active half.
## [2026-08-13] lint | Rechecked wiki links and unresolved review markers after the Pitru Smaran discovery/default ingest; no dead links or review markers found.
## [2026-08-13] ingest | Documented Puja Vidhi's occurrence-scoped preparation accordion and swipe-only Daily Bhakti-style conduct reader. Source/convention metadata stays mandatory for review but private in every UI; conduct uses bottom reader dots with no previous/next controls or swipe-helper copy, and completion has no dots.
## [2026-08-13] ingest | Completed the six-entry Puja Vidhi v1 registry: added Diwali Lakshmi-Ganesha, Ganesh Chaturthi Sthapana, Navratri Ghatasthapana, Karwa Chauth, and Maha Shivaratri alongside Satyanarayan (92 guided steps total), wired all festival-day hooks, pinned shipped katha/aarti refs and registry-wide source privacy, and retained pending canonical checks honestly in internal metadata.
## [2026-08-14] ingest | Recorded the Namkaran nakshatra-grid typography incident: `adjustsFontSizeToFit` on a multi-line, fixed-`lineHeight` label collapsed scattered iOS tiles (हस्त · चित्रा · स्वाती) to a few points while ignoring `minimumFontScale`. The in-tile launcher label now holds one fixed, multiplier-capped size across all 27 tiles, pinned by a test.
## [2026-08-14] ingest | Recorded the second Namkaran typography pass: the hero syllable's fixed 58/78 (54/70 on the share card) leading cropped above-shirorekha matras, and five micro labels carried Inter + Latin tracking over Devanagari. Heroes now pin no leading and rely on the container's minHeight; labels route through pillTextStyle. Also noted the name list's header clearance, which the first card's shadow was bleeding across.
## [2026-08-14] ingest | Merged main (PR #263's rashi peer-entry work) into the Namkaran typography branch. Recorded that the same tracked-Inter-over-Devanagari defect arrived on three new labels (`दिन की एक सम्भावना`, `राशि चुनें`, `राशि के नौ चरण`, `इस दिन की सम्भावना`) plus the new rashi screen, so the fix now covers them and the guard asserts over every Devanagari line on the screen instead of a named list. Added `indicSafeTag` to `utils/langType.ts` for mixed-case micro tags.
## [2026-08-14] ingest | Recorded the daily-panchang re-solve fix: the persisted day window ENDED at tomorrow (Home reads yesterday/today/tomorrow and persisted exactly those three), so the first launch after every midnight found its own "tomorrow" cold and — because `composeSolved` returns null on any miss — the Today strip fell back to `—` while a purge sweep, a multiGet and a fresh solve ran off the render path. Persistence was working; its window was one day short. New `panchangDayPrewarm.ts` rolls the window `PREWARM_DAYS` = 7 past today after the day's own solve lands, and the keyspace purge sweep is now once per session instead of once per cold range. Also recorded the two testing traps: engine-call assertions must be scoped to the days a surface RENDERS (the roll-forward legitimately solves beyond them), and the rollover gate fakes `Date` alone via jest's `doNotFake` because faking timers would deadlock the hooks' deferred chain — Maestro cannot express it, since a flow cannot move the device clock.
## [2026-08-14] ingest | Added the build-change derived-cache reset: `utils/derivedCacheReset.ts` compares a fingerprint (OTA updateId · runtimeVersion · bundle version · native build number, read in the isolated `utils/buildFingerprint.ts`) against the last build this device ran and clears the derived caches before anything reads them, so a bug baked into cached output cannot outlive the release that fixes it. Recorded the three things that make it safe rather than destructive: the sweep is an ALLOWLIST of recomputable engine output only (panchang days, observance scans, the widget write-dedupe key) with the chosen city/calendar system, OS-notification bookkeeping and all practice/follow/birth-detail/Pitru-Smaran keys explicitly excluded and pinned by a test that enumerates them; `App.tsx` registers at module scope while both caches await the gate on hydrate AND persist; and the expo-updates/expo-constants read stays out of the cache dependency graph because Jest cannot parse them. Also recorded the AsyncStorage-mock trap that surfaced while testing it — spying on a method that is already a jest.fn means a call-through recurses forever.
## [2026-08-14] ingest | Narrowed the build-change reset to computed-calendar output only, per the product intent: it sweeps the panchang/muhurat day solves (`panchang-days:`, legacy `muhurat-days:`) and the festival/vrat date scans (`observances:`), and nothing else. The widget's write-dedupe key was dropped from the allowlist — derived, but not calendar output, and redundant anyway since `WidgetCoordinator` re-plans on every pass and rewrites whenever the payload actually changes. It now sits in the test's must-survive list rather than being omitted, so a future change that starts sweeping it fails a test instead of passing silently.

## [2026-08-14] ingest | PRD-19 Phase 2B shipped: vidhi search rows (searchIndex + SearchScreen routing), Observance Detail "How to observe" card, Home DISCOVER spotlight, keep-awake in conduct (expo-keep-awake dep), 'vidhi' RoutineItemKind + AddToRoutineButton on recurring vidhis. Content: liturgy hand-offs (ganesh-stotram, durga-stotram, sandhya-deepam), inline Panchakshara on Shivaratri japa — registry now 96 steps / 12 mantras. Updated subsystems/puja-vidhi + subsystems/routine.

## [2026-08-14] gap | Canonical-edition sign-off for all six vidhis + Phase 3 shraddha vidhi remain blocked on an authoring environment with content egress (archive.org/DrikPanchang unreachable here; dated attempts recorded in each entry's canonicalEditionStatus).

## [2026-08-15] ingest | Closed the third and last shape of "today's panchang loads slower than the homepage". The first two reports were about the cache being cold when it shouldn't be (#265, persisted window ended at tomorrow) and about work queued in front of the read (#268, an InteractionManager gate and a whole-keyspace purge sweep). Both were fixed and the card still arrived late, because the remaining problem was WHEN the read was allowed to begin: the scope key needs both panchang preferences, and the calendar system was read lazily by its first subscriber — Home's Today strip, which only mounts after `AppReadyGate` opens the splash on the font-scale/language reads. So the multiGet answering "what is today's panchang" was the launch's THIRD serial storage round trip, behind a screen that had already painted everything else from bundled JS. New `panchangPrefs.ts` owns both preferences behind one memoized multiGet (they are read together because together they ARE the scope key) and `panchangLaunchPrefetch.ts` turns that into warm days from App.tsx module scope, concurrently with the splash gate; the location provider and calendar-system store both seed from that one read, so `useMuhurat`'s cache-only useState seed composes on the FIRST render. Recorded the four traps: the prefetch must stay hydrate-only (moving I/O earlier must not drag astronomy onto the launch path), it must warm exactly the shared `todayMuhuratDayKeys()` because `composeSolved` returns null on any miss, `panchangPrefs` may import only AsyncStorage + locations.ts since both the expo-location-bearing context and the prefetch import it, and the memoized read must be reset between Jest tests. The new suite carries a deliberate non-vacuity case asserting the first render IS blank without the prefetch.

## [2026-08-15] ingest | Fixed the reported "15 August is a Saturday but the app says Tuesday". The engine was innocent — `computePanchangForDate`'s vara and `buildCalendarMonth`'s cell order were both correct. Both calendars (the Panchang month grid and the Jyotish `CalendarDatePicker`) rendered all 42 cells into ONE `flexWrap: 'wrap'` row and produced columns with `width: ${100 / 7}%`. Yoga resolves percentages in 32-bit float, so on some container widths seven cells sum to a hair OVER the container (320.000008 pt inside 320 pt) and Yoga's bare `>` line-break test wraps the seventh — six columns under a seven-column header (the header has no flexWrap and kept all seven), sliding every date off its weekday. 15 Aug 2026 is grid index 20, `20 % 6 == 2` → column 2 → मंगलवार. Verified against the real Yoga (`yoga-layout`): the month grid broke at 390/411 dp and the picker at 375 dp, while 360/393/412/428 were fine, which is why it read as intermittent. New `calendarWeeks()` chunks the month into six rows of seven `flex: 1` cells, so the column count is structural at any width (checked 200–600 dp). Recorded in subsystems/panchang Gotchas alongside the Namkaran tile-width sibling, and pinned by `calendarGrid.test.ts` (column index == getDay() across 96 months + a source guard on both files) and two CalendarDatePicker render cases.

## [2026-08-16] ingest | Added a nationwide pincode tier to the location picker, growing the resolvable locations from 394 to 18,860. Origin: Kolhapur — a Shakti Peeth already pinned in the Theerth catalog — was absent from `MAJOR_CITIES`, so a Kolhapur user's GPS fix snapped to Panaji, 135 km away and in another state, and got Goa's sunrise. The picker now takes a 6-digit pincode as well as a city name. Data comes from joining two npm datasets (the container's egress is package registries only, same constraint as the Rajasthan tehsil pass): `india-pincode-search` for pincode→state/district/taluka and `pincode-lat-long` for coordinates, 99.0% joining, gated on no-geocode / 0,0 null island / India bbox to 18,466 rows, reproducible via a committed `scripts/build-pincodes.mjs`. Unlike the Rajasthan postal extract this data needed no district-box gate — only 18 coordinates are shared by more than one pincode, so the mass snapping-onto-district-HQ that forced the percentile box there is simply absent. Three findings shaped the design and are the reason it is not just "a bigger CITIES". (1) COVERAGE IS UNEVEN AND CAN REGRESS USERS: the geocoder carries 1 of the directory's 207 Jammu & Kashmir pincodes and 19 of 48 in Arunachal, so a Srinagar fix's nearest pincode is 156 km away in Doda while the bundled `srinagar` entry sits on top of it — `snapToNearestLocation` therefore takes whichever of the two tiers is genuinely closer (ties to the city, which has the better label) rather than always preferring a pincode, and both directions are pinned. (2) NO HINDI PLACE NAMES EXIST in either dataset and `transliterate.ts` runs Devanagari→gu/kn rather than Latin→Devanagari, so pincodes cannot satisfy the `nameHi` Devanagari invariant every `City` row carries; instead of machine-transliterating 18k names into plausible-but-wrong spellings the tier labels itself `४१६००१ · महाराष्ट्र` from Devanagari digits plus one of 36 hand-authored state names, which is real Devanagari and so transliterates into gu/kn for free. (3) BOTH directory name fields are needed and both are misleadingly named — its `city` is the district and its `district` is the taluka, and neither alone is the familiar name (416001 is Kolhapur/Karvir, 781001 is Kamrup/Guwahati), so both ship and the row collapses them when equal. The 700 KB table sits behind a lazy `require` and is warmed only after the picker opens, so it never reaches the launch path #268/#269/#272 cleared; `panchangPrefs` imports only the `isPincodeCityId` regex and validates a stored `pin-` record structurally (range-checked labels + coordinates) because it cannot look one up. Two assumptions were measured rather than trusted: elevation is inherited from the nearest bundled city and can be 543 m wrong (Kolhapur takes coastal Panaji's 7 m), but against `astronomy-engine` that moves sunrise 7.5 seconds, so no elevation dataset is worth 18k rows; and the dataset is vintage enough to spell Chhattisgarh "CHATTISGARH", predate the Ladakh split, and put Kolhapur's own centroid 19 km off — all cosmetic, since coordinates are what feed sunrise. Two existing GPS tests legitimately changed contract (a Varanasi and a Mumbai fix now resolve to nearer pincodes) and were updated rather than worked around. Full suite green: typecheck, 1108 Jest, 233 engine, 71 data. E2E not run — no simulator or `maestro` binary in this container.

## [2026-08-16] ingest | Fixed Puja Vidhi's back-navigation: Home's DISCOVER card (and the search rows / routine items) reached the catalog with a cross-tab `navigate('PanchangTab', …)`, so back popped to the Panchang calendar — a tab the user never chose, whose default mode carries no vidhi door, leaving the flow they had just opened unreachable. The three vidhi routes are now declared once in a shared `VidhiStackParamList` that both `HomeStackParamList` and `PanchangStackParamList` intersect, and registered in BOTH navigators; the screens type against the shared list so one component serves both mountings. Every door now pushes in place. The one genuine cross-stack hop left — conduct → shipped readers, which live only on the Home stack — goes through the new `navigateToHomeStackTarget`, which checks `getState().routeNames` and pushes in place or falls back to `HomeTab`. Recorded as RULEBOOK §6.0 (the general rule for a flow with doors on two tabs), design.md §62, and updated subsystems/puja-vidhi + subsystems/routine.

## [2026-08-16] ingest | Audited every Home entry point for the vidhi back-navigation defect and found a worse, distinct one: the DISCOVER widgets spotlight hand-rolled `navigate('MoreTab', { screen: 'WidgetGallery' })` with NO `initial: false` — the only such site in the codebase, while 10+ others carried it. The tab bar is lazy, so tapping that card before ever opening More made WidgetGallery the More stack's INITIAL route: dead back button, and the whole hub (Wishlist, Profile, Reminders, Japam Alarms, Pitru Smaran) unreachable until app restart. The Pitru Smaran day chip — which renders on Home's Today strip, a common first touch of the More tab — had the same defect on its detail hand-off. Added `moreTabTarget` mirroring `panchangTabTarget`, routed all six MoreTab callers through it, and both helpers now type `params` as required-when-the-route-requires-it (the old `params?:` return only compiled because most callers went through an untyped `useNavigation<any>()`). New `navigation/__tests__/tabTargets.test.ts` scans all source for the hand-rolled form; HomeTab is exempt (eagerly mounted as the first Tab.Screen) and that exemption is asserted against TabNavigator's order rather than assumed. Verified non-vacuous by reintroducing the bug. The other Home cross-tab cards (व्रत, मुहूर्त, कुंडली tiles; Pitru Smaran and Daily Verse spotlights) are NOT the vidhi defect: their landing screens each carry a door back into the flow (Panchang calendar hosts MuhuratFinderDoor and the day's observance cards; MoreHome has rows for both More flows), so back is recoverable — recorded as a deliberate finding, not fixed. RULEBOOK §6.0 (new) states the helper rule; §6.0.1 is the former §6.0. design.md §32 gained a "where a card's target lives" para.

## [2026-08-18] ingest | Built PRD-16 Phases 3+4 (lagna-grade windows; personalised Tarabala/Chandrabala), completing the Event Muhurat Finder short of Phase 5/Vivah. Phase 3: instead of adopting the PRD's seeded-root-search sweep, a closed-form ascendant (`ascendantSiderealLongitude` in kundali.ts — the standard RAMC/obliquity formula IS the rising ecliptic-horizon intersection computeLagna bisects for; agreement verified <1e-12° over 5,000 samples) made the lagna sweep nearly free: 12–13 spans/day at ~200 trig evals, measured +3.6% on computeDayInputs against the ≤25% gate, so `DayInputs.lagnas` persists (cache v3). Window pass now splits at lagna AND anga boundaries before grading (kshaya-aware; <24-min parts dropped, never clipped) — the 17 Aug 2026 morning Amrit now *starts* at its 6:07:57 lagna boundary because the 4-min lead falls under the floor. Two traps worth remembering: ① the hora layer is "tie-break only" and the first implementation sorted (tier, hora) — which silently flipped 17 Aug's best window from Amrit to Abhijit and broke three pinned suites; the correct key order is tier → preferred lagna → window priority → hora, i.e. a tie-break breaks TIES, it does not reorder the Phase-1 priority. ② Landing late-onset Vishti (PanchangData.lateVishti — the karana after the sunrise karana, one extra bisection on ~13% of days) re-flipped a published Phase-2 pin: 27 Aug 2026 was "offered via its Purnima windows", but Purnima's first half IS Vishti (the eve-of-Raksha-Bandhan Bhadra, 9:09 AM–9:32 PM) — Phase 2's flip table was itself blind to the afternoon Bhadra, so the pin was rewritten to assert the exclusion, not deleted. The Phase-1 drikfixture debt landed with rows from the PUBLISHED Nov-2026 Griha Pravesh list as recorded in the PRD/TRD (this container has no content egress — drikpanchang.com is proxy-blocked), and the known Chaturmas-reading divergence (11/14 Nov listed publicly, excluded by the shipped tithi-span reading) is asserted AS a divergence naming `chaturmas`, so a future convention change fails the test loudly. Lagna preference tables ship EMPTY (inert) with candidate rows in conventions/muhurat-lagna-v1.md; दिशा शूल ships populated-DRAFT (यात्रा needs it to function — the Upanayana-masa precedent). Phase 4: taraChandraBala.ts is deliberately NOT the Guna Milan Tara koota (bidirectional half-scores) — one-directional, nine named classes, जन्म contested; a guard test asserts the two conventions disagree on the जन्म case so nobody "unifies" them. The strip annotates only; share-card and reminder-copy absence are pinned. Jest gotcha: the day detail's cold-store solve under the instrumented engine can exceed 100ms, so MuhuratPersonalStrip.test polls (waitFor) instead of sleeping a fixed settle — a fixed 120ms still raced. Docs synced same PR: design.md §60, RULEBOOK §17.2/9/10/12, both PRD status lines, this page. Maestro phase3/phase4 smokes authored; no simulator in this container — device runs owed.

## [2026-08-19] ingest | Closed the standing content-egress gap across the recent PRDs. All eight Upvas entries now carry two-plus sources and are verified; monthly Shivaratri/Janmashtami rules no longer inherit annual procedures. Opened Drik, Dharma Sindhu chs. 17–18 and B. V. Raman to correct six conservative Event Muhurat rule intersections and replace stale URLs, while retaining `verified:false` until all 12 occasions have independent city/year goldens. Namkaran now pins two complete 108-pada tables plus Gana/deity references, but keeps the human variant decision and 12+12 corpus gates closed. Opened Gita Press code 592 to verify four puja canonical chapters, bounded Karwa Chauth and Satyanarayan edition gaps precisely, and added the Shraddha/Tarpana source dossier distinguishing narrow tila-tarpana from full parvana-shraddha. Typecheck, 252 engine tests and 75 data/content tests passed.

## [2026-08-19] lint | Rechecked all indexed wikilinks, page statuses, contradiction markers and prior gap entries after the source ingest. No dead/orphan indexed pages, needs-review pages or contradiction markers found. The 2026-08-14 content-egress gap is superseded: access is restored; remaining gates are two exact-edition/editorial reviews, Event Muhurat goldens, Namkaran variants/corpus, and the Shraddha scope decision rather than blocked networking.

## [2026-08-20] ingest | Registered the narrow, source-backed household tila-tarpana remembrance as the seventh Vidhi (106 steps / 12 mantras overall), with no inline formula or claim of complete Shraddha. Wired personal-detail, Pitru-Paksha overview and public Sarvapitri doors, personal-tithi occurrence scoping, More-stack navigation, local Gita 15/2 hand-offs, search/catalog discovery, tests and the extended Pitru Maestro flow.

## [2026-08-20] lint | All 12 indexed wikilinks resolve to the 12 current pages; refreshed Puja Vidhi, Panchang and index coverage; no needs-review/stale pages or open contradiction markers. The 2026-08-19 Shraddha scope decision is superseded by the implemented narrow household boundary; device Maestro evidence remains a release gate rather than a sourcing gap.

## [2026-08-20] ingest | Native verification: generated a fresh iOS Release build from this checkout and passed the full extended Pitru Maestro flow on iPhone 17 Pro Max / iOS 26.4. The run covered the sourced guide, Gita 15 hand-off, local Back to conduct at step 8/10, guide/person Back chain, cache re-entry, delete cleanup and civil-date branch. Android remains the separate device gate.

## [2026-08-20] lint | Rechecked the indexed pages and integration wording after native verification; all 12 links resolve, no needs-review/stale page or open contradiction marker exists, and iOS-pass versus Android-pending evidence is stated consistently.

## [2026-08-24] ingest | Built multi-person Jyotish: birth details are now a ROSTER (`@vedansh:kundali-profiles:v1` = `{activeId, people[]}`, cap 8) with one active selection that every personalised surface reads — Kundali result, the Jyotish landing's Rashifal card and chart glance, Daily Rashifal, and the muhurat finder's आपके लिए Tarabala/Chandrabala strip. Shape follows the existing split: `panchang/birthProfiles.ts` is the pure model (it also became the home of `BirthProfile`/`validateBirthProfile`/`birthProfileToInput`/`parseStoredBirthProfile`, re-exported from `useKundali.ts` so no importer moved) and `birthProfileStore.ts` is AsyncStorage + one snapshot + subscribers + a serialized write queue — the panchangDayStore ⇄ panchangDayCache pattern one layer up. `useKundali()` deliberately KEEPS its single-profile shape (`profile` is the active person; `saveProfile`/`clearProfile` act on them), so Guna Milan autofill and the landing compiled unchanged, and grew `people`/`activeId`/`selectPerson`/`addPerson`/`updatePerson`/`removePerson`; `useBirthProfileRoster()` is the shared subscription, which is how a chip tap on the landing reaches `useMuhuratBala` on the same render instead of via its old focus listener. Four decisions worth remembering. ① MIGRATION DELETES THE LEGACY KEY: a valid `@vedansh:kundali-birth-profile:v1` becomes person one and the old record is removed, because leaving it would keep a readable copy of their birth details that survives removing that person; an UNREADABLE legacy record is never deleted and still reaches the shipped corrupt-profile recovery state (the strip suite pins that case through the migration path). ② A FAILED READ CANNOT BE MEMOIZED BY `hydrated`: the store publishes `hydrated: true` on a read failure so surfaces show recovery rather than spinning, which made `hydrated` a false retry gate — a test written for the `panchangPrefs` lesson caught it, and a separate `lastReadFailed` flag is the fix. ③ "Who is active" must be resolved INSIDE the write queue (`saveActivePerson`/`removeActivePerson`), or a read-then-write saves into a person another surface just removed. ④ COPY IS GATED ON `people.length > 1` AND a name, never on the roster existing, so a one-person device is byte-identical to what shipped ("आपके लिए", "From your Kundali") while a shared phone names the person — the banned-implementation-copy engine test also caught the cap message's "इस उपकरण पर" and it was rewritten. UI is one new control, `components/PersonChips.tsx` (chips not a dropdown: whose chart it is must be visible state, 44 pt floor, 1.25 font cap, name-or-birth-date label); `+ जोड़ें` opens a blank `Kundali { newPerson: true }` form beside the saved people, removing one of several lands on a survivor, removing the last returns the guest state. Green: typecheck, 158 Jest suites / 1176 tests (new `birthProfiles.jest.test.ts` + `MultiProfileJyotish.test.tsx`, extended `MuhuratPersonalStrip`/`KundaliExperience`/`derivedCacheReset`), 296 engine, 23 widget, 75 data, lint 0 errors. `.maestro/multi-profile-jyotish-smoke.yaml` authored; no simulator or maestro binary in this container, so the iOS/Android device runs are owed. Docs synced same change: design.md §51 + new §51a, RULEBOOK §14.4/§14.5/§17.12, subsystems/panchang.

## [2026-08-25] ingest | Built PRD-23 Bhog, Naivedya and Vrat Food: ten verified bilingual profiles with two-plus independent sources, private provenance and explicit regional/sectarian variance. Added the verified-only `bhogId` hook, shared Observance/Vidhi guidance panel, a namespaced occurrence-scoped kitchen checklist that does not alter samagri progress, and shared-list integration. Source adjudication rejected the candidate brief's universal Tulsi-plucking and durva prohibitions, preserved the ordinary-Ganesha versus Ganesh-Chaturthi Tulsi exception, and kept Shivaratri abhisheka ingredients distinct from food. Added [[bhog-naivedya]], RULEBOOK §21, design §65.1, PRD/tests and an extended Vidhi Maestro path; iOS/Android device runs remain the release gate.

## [2026-08-25] lint | Rechecked the 13 indexed pages and all wikilinks after PRD-23 ingest: no orphan/dead link, needs-review page, duplicate subsystem or contradiction marker found. Historical gap entries remain as append-only evidence and are superseded by their later ingest/lint records where resolved; device Maestro evidence is the only new PRD-23 release gate.

## [2026-08-25] ingest | Closed PRD-23's verification gates: the full mobile command passed typecheck, 25 widget tests, 1,217 Jest tests in 163 suites, 303 engine tests and 75 data tests; lint stayed at zero errors. The same [[bhog-naivedya]] Vidhi flow passed separately on iOS 26.4 and Android 16/API 36, with Android using an embedded release bundle and disabled network to exclude unrelated Metro or OTA content. The deterministic Panchang catalog door now keeps its final Puja tile reachable, and the E2E verifies independent Bhog-kitchen versus ritual-samagri checked states through conduct-mode completion.

## [2026-08-25] lint | Refreshed [[bhog-naivedya]] and [[e2e-verification]] with the completed cross-platform evidence; indexed-page and wikilink checks remain clean with no current review, duplicate, orphan, dead-link or contradiction finding.

## [2026-08-26] ingest | Completed all PRD-23 content phases: 39 verified bilingual profiles now cover every genuine vrat/upavas rule (68/68) through 108 published URLs on 50 domains. Added recurring, annual, advanced and regional guidance while keeping fast food, naivedya, ancestor offerings, abhisheka and parana distinct. Corrected Chandra Darshan, Ishti/Anvadhan and Shraddha Dates from fasting categories to ritual-calendar records; Shraddha retains the narrow sourced `pitru-offering` profile. Added an exhaustive live-catalogue coverage gate so any future vrat/upavas row without verified content fails tests.

## [2026-08-26] lint | Rechecked 15 wiki pages and 60 wikilinks after the PRD-23 all-phases ingest: no dead link or current review/contradiction marker was found. Full static/content validation passed against the merged `main` (304 engine and 77 data/content tests), and the expanded bundle passed `vidhi-smoke.yaml` on iOS 26.4 after one XCUITest launch-driver retry. Android all-phases execution remains a separate release gate; the 2026-08-25 Android result covers the earlier v1 bundle only.
## [2026-08-25] ingest | Fixed the Kundali Dasha card reporting the Mahadasha's window under an Antardasha headline: `KundaliScreen` computed its date range, progress bar and elapsed/left from `currentDasha.maha`, so a Rahu Antardasha displayed Mercury's full 17-year span (`29 Apr 2016 — 29 Apr 2033`, `10 y 3 m elapsed`). Both spans now get their own labelled line, all three readings track `antar ?? maha`, and `formatDuration` gained a sub-month day branch. Synced design.md §51, subsystems/panchang.md (Dasha detail + new gotcha), the Jest regression and the Maestro smoke.

## [2026-08-25] ingest | Dasha current-period card now shows BOTH running windows: since Mahadasha and Antardasha are nested and simultaneously current, the card renders two labelled rows — each with its own dates, progress bar and elapsed/left (testIDs `dasha-progress-maha`/`dasha-progress-antar`) — plus the earlier level captions on the Antardasha chip row and the MAHADASHA TIMELINE eyebrow. Synced design.md §51, subsystems/panchang.md, Jest and Maestro.
## [2026-07-31] ingest | Added the source-candidate manifest for 20 missing Chalisas and 19 missing Aartis that map only to the existing 21 deity ids. Captured identity anchors, two-source candidates where available, and explicit holds for variant, provenance, and rights risks; competitor content remains taxonomy-only and no reader/library data was activated.
## [2026-07-31] lint | Rechecked the 10-page wiki after the content-parity ingest: no dead wikilinks, orphan pages, or needs-review pages.
## [2026-07-31] ingest | Expanded the existing-deity content-parity research beyond Chalisa and Aarti. Captured an 826-row competitor catalog snapshot, separated 768 other-form records, identified present/partial/variant-review states, documented semantic deity-mapping exclusions, and proposed a one-item-per-existing-deity first acquisition wave. No devotional text or app registry was activated.
## [2026-07-31] ingest | Integrated Rudrashtakam as the first independently sourced parity acquisition, reusing the existing Shiva deity, Ashtakam registry, shared reader, discovery/routine/search/progress surfaces, and Shiva background. The complete 8-verse hymn plus phalashruti has line-matched IAST and original editorial Hindi/English meanings; data/type/reader gates and first-to-last iOS + Android reader flows passed.
## [2026-07-31] lint | Rechecked the 10-page wiki after Rudrashtakam integration: no dead wikilinks, orphan pages, needs-review pages, or contradictions. The existing shared-reader-body gap remains intentionally tracked.
## [2026-08-26] lint | Rechecked the wiki after merging current main into the Rudrashtakam content-parity branch: preserved both the Ashtakam and Vālmīki reader documentation, with no dead wikilinks, orphan pages, needs-review pages, or contradictions introduced by conflict resolution.

## [2026-08-30] ingest | Integrated 12 approved 1024×1024 WebP background plates: dedicated Lakshmi, Surya, Radha–Krishna, Kubera, Ganga, Parvati/Bhavani, Narasimha–Prahlad, Dattatreya, Shani, Kali and symbolic Navagraha art plus the Kavacham category plate. Wired deity and reader registries, corrected Konark to the Surya deity mapping, and added exact identity assertions; the 12 production assets add 2,314,372 bytes (2.207 MiB) before store-level packaging. Refreshed [[overview]] with the typed background registry contract.

## [2026-08-30] ingest | Fixed Sankashti Chaturthi and Karwa Chauth resolving one day late. Both are moonrise (chandrodaya-vyapini) vrats — the fast ends with the moon sighting and arghya — but the matcher had only a sunrise rule, so Bhadrapada 2026 landed on 1 Sep (moonrise 9:22 PM, in Panchami) rather than 31 Aug (moonrise 8:39 PM, inside Chaturthi). Added `ObservanceRule.dayRule` with a `chandrodaya` case (first-of-two-moonrises dedupe, udaya fallback when no moonrise falls inside the tithi) over a gated `tithiAtMoonrise` in `engine.ts`, retagged the two rules, and regenerated `precomputedObservances.ts` — Sankashti moved in 6 of 12 lunations in 2025 and 5 of 13 in 2026, Karwa Chauth only in 2027 and 2031, and nothing else changed. Also closed the "तिथि तक H:MM leaves the rest of the day unnamed" report: `successorTithiToday` adds a handover line to the Muhurat glance-card kicker and the Tithi tile, and a moonrise-vrat line names the parana instant on chandrodaya observance cards. Synced design.md §33, RULEBOOK §23 (new), `panchang/VERIFICATION.md`, and this page.
## [2026-08-30] ingest | Added the two missing Teej vrats: the catalog carried only Hartalika Teej (Bhadrapada Shukla 3), so Hariyali Teej (Shravana Shukla 3) and Kajari/Badi Teej (Bhadrapada Krishna 3 — 31 Aug 2026, the reported "no vrat on Tritiya" day) rendered "no vrat or festival". Both are udaya-tithi rules verified against published 2024–2026 dates (Hariyali 7 Aug 24 / 27 Jul 25 / 15 Aug 26; Kajari 22 Aug 24 / 12 Aug 25 / 31 Aug 26), each with a sourced bhog profile (ghewar; neem pooja + sattu/Satudi parana) to satisfy the PRD-23 coverage gate (now 70/70 rules, 41 profiles). Bumped observanceCache CACHE_VERSION 2→3 — it also covers the chandrodaya matcher change, which had shipped without the bump — and regenerated precomputedObservances.ts (only the two new ids added). Synced the bhog wiki page counts.
## [2026-08-30] ingest | Added Bahula Chaturthi (Bol Choth) — the named children's-welfare vrat on Bhadrapada Krishna Chaturthi that the generic monthly Sankashti card did not convey. Chandrodaya day rule like its Sankashti sibling (fast concludes after the evening Godhuli cow-and-calf puja and moonrise; published 12 Aug 2025 / 31 Aug 2026 match the engine), with a sourced bhog profile (millet fare, milk products abstained in honour of the cow) — coverage now 71/71 rules, 42 profiles. Precomputed table regenerated (only the new id added); RULEBOOK §23, VERIFICATION.md and the panchang/bhog wiki pages updated to name all three chandrodaya rules.
## [2026-08-30] ingest | Fixed the madhyahna half of the Class B day-shift: Ganesh Chaturthi, Ram Navami, and the monthly Vinayaka Chaturthi are fixed by the tithi at MIDDAY (sthapana/janma worship), and udaya matching named the next day whenever the tithi opened shortly after sunrise — Ganesh Chaturthi 2026 resolved to 15 Sep against the published 14 Sep, Ram Navami 2026 to 27 Mar against 26 Mar. Added `tithiAtMadhyahna` (sunrise–sunset midpoint, gated like `tithiAtMoonrise`, shared sunset memo) and generalized the chandrodaya matcher into `matchesInstantVyapiniRuleOnDate`, parameterized by the instant solver. Published-date anchors pinned for 2024–2026 plus the Vinayaka-rides-Ganesh-Chaturthi invariant; precomputed table regenerated (only the three rules moved: ganesh 2026, ram-navami 2026/2028, ~20 Vinayaka lunations). verify:observances Class B shrank 9 → 7 (only pradosh Diwali/Dhanteras and nishita Maha Shivaratri remain). Synced RULEBOOK §23, VERIFICATION.md, this page.
## [2026-08-30] ingest | Titled Sankashti day cards with the occurrence's published name: the generic "संकष्टी चतुर्थी व्रत" hid that 31 Aug 2026 is the big Heramba Sankashti. New pure `sankashtiNames.ts` maps the occurrence day's purnimant month to the Ganapati form (हेरम्ब/लम्बोदर/वक्रतुण्ड …, विभुवन for an adhik lunation, अंगारकी flag on Tuesdays), verified against the published lists including Vibhuvana on 3 Jun 2026 and Angarki on Tue 29 Sep 2026; the day-panel ObservanceCard uses it with a rule-name fallback, all other surfaces unchanged. Synced design.md §33.5 and the panchang wiki page.
## [2026-08-30] ingest | Added Bhadwa Chauth (चौथ माता व्रत) — the Rajasthani identity of Bhadrapada Krishna Chaturthi, distinct from the Gujarati Bol Choth on the same tithi: a suhag vrat to Chauth Mata and Vinayak Ji (Chauth Ka Barwara, Sawai Madhopur), day fast concluded with the evening moon arghya and the bayana to the mother-in-law. Chandrodaya day rule shared with its three siblings (equality with Bahula pinned 2024–2028), sourced churma-laddu bhog profile (coverage 72/72, 43 profiles), precomputed table regenerated with only the new id. RULEBOOK §23.2, VERIFICATION.md and both wiki pages updated to the four-rule chandrodaya set.

## [2026-08-31] ingest | PRD-27 शुभ योग shipped: pure `shubhYoga.ts` (सर्वार्थ/अमृत सिद्धि vāra×nakshatra tables, रवि योग inclusive Sun→Moon count, द्विपुष्कर/त्रिपुष्कर tithi×vāra×nakshatra) over kshaya-aware anga segments of the vāra-day; annotate-only pinned structurally (eventMuhurat must not import it). Surfaces: ShubhYogaCard on the Panchang day panel + finder day detail, शुभ योग rows on MuhuratCardBody, chips on finder result cards via the shared MuhuratChip (dosha tone reused on excluded answers). The nitya-yoga field relabelled नित्य योग everywhere (the सिद्धि naming collision). All window ends through formatEndInstant; 26:12 banned. Tables DRAFT (verified:false, release-gating) in conventions/shubh-yoga-v1.md — snippet-sourced, §10 outstanding. Synced design.md §33/§60/§69, RULEBOOK §24, [[panchang]]; suites shubhYoga.test.ts + ShubhYogaCard.test.tsx; .maestro/shubh-yoga-smoke.yaml authored (device run owed).
## [2026-09-02] ingest | Built PRD-41 जिज्ञासा (Ask Vedansh), Phases 0–3: `mobile/src/ask/` deterministic resolver (fold with abugida inherent vowel + Hindi schwa deletion, registry-derived lexicon with recurring-observance classes, specificity floor, hard slot gate, stance guard that declines predictive questions), 13 intents over existing engines, 199-question golden corpus at 100%/0 wrong, launch-path import guard; answer-first SearchScreen with AskAnswerCard/AskAbstainCard and rotating placeholder; TodayVidhanScreen briefing of standing questions via Home DISCOVER card; ObservanceDetail ask-from-context. Recorded the two false-tag classes the prototype found (class-vs-instance, over-eager stemming) and the Hindi/Sanskrit fold gotcha. design.md §71, RULEBOOK §25, wiki [[ask]].
## [2026-09-02] ingest | Removed full verse-pool materialisation from Home routines, widgets, reminders, and Daily Bhakti; cold widget links now mount their target tab directly.
## [2026-09-03] query | Q4 2026 planning: reviewed every shipped feature (source + design.md §1–§71 + RULEBOOK §0–§25 + the 11 subsystem pages + PRDs 01–41) and audited where the app still loses or misleads a loyal user. Findings recorded in `docs/roadmap/2026-Q4-roadmap.md`: (1) ~40 user-state AsyncStorage keys and no backup/restore anywhere — PRD-06's backup third was never built, PRD-29 shipped a one-way export as a stopgap; (2) `locations.ts` is India-only and `snapToNearestLocation` snaps any GPS fix on Earth to the nearest Indian pincode/city with no distance bound, while the engine already accepts `civilTimeZone` and the day store forbids it (`civilTimeZone?: never`); (3) no dark palette behind `ThemeMode`, no sleep timer across the three arbitrated sound sources; (4) PRD-26/PRD-28 candidates still have no PRD file. New PRDs: 42 संचय, 43 प्रवासी, 26, 28, 04-P2; सङ्कल्प re-reserved as PRD-44 (20 was consumed by the horoscope PRD), more scripts reserved as PRD-45. **Product decision same day: the Q4 slate is PRD-42 + PRD-43 only** — 26, 28 and 04-P2 carry a `Deferred` status and stay build-ready; PRD-43 Phase 2 pulls into December on a conditional 1.6.1; the Diwali pradosh day-rule fix PRD-28 owned moves to the debt line (due before 7 Nov). Docs-only change; no source touched.
